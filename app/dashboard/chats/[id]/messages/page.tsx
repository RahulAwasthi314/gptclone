export default async function Page() {
    // get all messages of chatId: and segregate them into same message id, and their respective version id
    // then show the messages
    return <div>show the first message and response of each version array have left and right arrow to traverse to last and first version</div>
}